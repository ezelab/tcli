# Notes

tcli, text cli is a cli for coding agents to convert any file format to text and get its metadata. tcli will be able to install itself as a skill. tcli will use tika to process documents to text and will also support ocr via tesseract, tcli will output a self contained binary for each of linux, windows and macos